# Crie uma função que imprima a quantidade de dígitos de um determinado
# número inteiro informado.
def quantDig (digito):
    valor = len(digito)
    return valor

numero = input("Digite um numero:")

print(quantDig(numero))